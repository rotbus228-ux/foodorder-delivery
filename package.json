{
  "name": "foodorder-delivery-server",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
