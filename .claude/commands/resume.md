อ่านไฟล์ CLAUDE.md ในโปรเจคนี้ก่อน แล้วสรุปให้ครบดังนี้:

1. **โปรเจคนี้คืออะไร** — ชื่อระบบ, ใช้ทำอะไร
2. **เว็บที่ deploy** — URL Frontend และ Backend (ถ้ามี)
3. **สถานะปัจจุบัน** — ทำอะไรไปแล้ว, อะไรยังค้างอยู่
4. **โฟลเดอร์สำคัญ** — frontend/src, backend, supabase-setup.sql
5. **ขั้นตอนถัดไป** — ถามผู้ใช้ว่าจะทำอะไรต่อ

หลังสรุปเสร็จให้พร้อมรับคำสั่งได้เลย ไม่ต้องถามอะไรเพิ่ม
